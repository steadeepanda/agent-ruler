{
  "name": "agent-ruler-docs",
  "private": true,
  "version": "0.1.9-2",
  "description": "VitePress docs for Agent Ruler Control Panel help",
  "type": "module",
  "scripts": {
    "docs:sync": "node scripts/sync-curated-docs.mjs",
    "docs:dev": "npm run docs:sync && vitepress dev docs --host 127.0.0.1 --port 5173",
    "docs:build": "npm run docs:sync && vitepress build docs",
    "docs:preview": "vitepress preview docs --host 127.0.0.1 --port 4173"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  },
  "overrides": {
    "esbuild": "^0.25.0"
  }
}
