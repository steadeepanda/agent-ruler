import { _ as _export_sfc, o as openBlock, c as createElementBlock, ag as createStaticVNode } from "./chunks/framework.UCBG75GM.js";
const __pageData = JSON.parse('{"title":"Security Policy","description":"","frontmatter":{"title":"Security Policy"},"headers":[],"relativePath":"security/security-policy.md","filePath":"security/security-policy.md"}');
const _sfc_main = { name: "security/security-policy.md" };
function _sfc_render(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("div", null, [..._cache[0] || (_cache[0] = [
    createStaticVNode('<blockquote><p>Synced automatically from <code>SECURITY.md</code>. Edit the source file and run <code>npm --prefix docs-site run docs:sync</code>.</p></blockquote><h1 id="security-policy" tabindex="-1">Security Policy <a class="header-anchor" href="#security-policy" aria-label="Permalink to &quot;Security Policy&quot;">​</a></h1><h2 id="supported-versions" tabindex="-1">Supported Versions <a class="header-anchor" href="#supported-versions" aria-label="Permalink to &quot;Supported Versions&quot;">​</a></h2><p><code>v0.1.7</code> (Linux-first) is currently maintained.</p><h2 id="reporting-a-vulnerability" tabindex="-1">Reporting a Vulnerability <a class="header-anchor" href="#reporting-a-vulnerability" aria-label="Permalink to &quot;Reporting a Vulnerability&quot;">​</a></h2><p>Please report vulnerabilities privately by opening a security advisory or contacting maintainers directly before public disclosure.</p><p>Include:</p><ul><li>affected version/commit,</li><li>reproduction steps,</li><li>impact,</li><li>suggested mitigation (if known).</li></ul><h2 id="response-expectations" tabindex="-1">Response Expectations <a class="header-anchor" href="#response-expectations" aria-label="Permalink to &quot;Response Expectations&quot;">​</a></h2><ul><li>Initial triage target: 72 hours.</li><li>Confirmed high-severity issues are prioritized for patch release.</li></ul><h2 id="security-design-principles" tabindex="-1">Security Design Principles <a class="header-anchor" href="#security-design-principles" aria-label="Permalink to &quot;Security Design Principles&quot;">​</a></h2><ul><li>Deterministic policy engine.</li><li>Least privilege + default deny.</li><li>Explicit approval boundaries for high-risk operations.</li><li>Append-only receipts for forensic traceability.</li><li>Runtime state separation from source tree by default.</li><li>UI/API command paths reuse the same deterministic policy/runner controls as CLI.</li></ul><h2 id="runtime-data-handling" tabindex="-1">Runtime Data Handling <a class="header-anchor" href="#runtime-data-handling" aria-label="Permalink to &quot;Runtime Data Handling&quot;">​</a></h2><p>By default, mutable runtime artifacts are stored under:</p><ul><li><code>~/.local/share/agent-ruler/projects/&lt;project-key&gt;/</code></li></ul><p>This includes workspace outputs, approvals, receipts, staged-export state, and quarantine artifacts.</p><p>Recommendations:</p><ul><li>Keep runtime root on encrypted storage for sensitive workloads.</li><li>Restrict runtime root permissions to the executing user.</li><li>Do not share runtime directories across untrusted users.</li></ul><h2 id="bypass-and-degraded-mode-warnings" tabindex="-1">Bypass and Degraded Mode Warnings <a class="header-anchor" href="#bypass-and-degraded-mode-warnings" aria-label="Permalink to &quot;Bypass and Degraded Mode Warnings&quot;">​</a></h2><ul><li>Unsafe bypass (<code>--bypass</code>) for import/export/deliver is opt-in and should be emergency-only.</li><li>Degraded confinement mode (<code>allow_degraded_confinement=true</code>) is opt-in and should only be used when host policy blocks bubblewrap namespaces.</li><li>Both modes reduce security posture and are explicitly marked in receipts.</li></ul><h2 id="current-security-limitations" tabindex="-1">Current Security Limitations <a class="header-anchor" href="#current-security-limitations" aria-label="Permalink to &quot;Current Security Limitations&quot;">​</a></h2><ul><li>v0.1.7 enforcement requires launching via Agent Ruler runner/API flows.</li><li>Linux confinement currently uses user-space namespace controls (<code>bubblewrap</code>) without kernel driver support.</li><li>Some hosts/VMs may restrict unprivileged user namespaces (for example <code>setting up uid map: Permission denied</code>).</li><li>Windows enforcement is planned for a later version.</li></ul><h2 id="prompt-injection-and-internet-access" tabindex="-1">Prompt Injection and Internet Access <a class="header-anchor" href="#prompt-injection-and-internet-access" aria-label="Permalink to &quot;Prompt Injection and Internet Access&quot;">​</a></h2><p>Prompt injection risk is treated as a policy-governed execution risk, not as trusted instruction following.</p><p>Current protections in v0.1.7:</p><ul><li>Network default-deny via policy (<code>rules.network.default_deny=true</code> by default).</li><li>Bubblewrap network namespace isolation (<code>--unshare-net</code>) when default-deny is active.</li><li>Host allowlist enforcement for explicit command-line egress checks, including internet-enabled mode when an allowlist is configured.</li><li>Deterministic command preflight URL host extraction before launch.</li><li>Upload-style exfil preflight detection (for example <code>curl --data</code>, <code>--data-binary</code>, upload flags, stream upload patterns) with approval requirement.</li><li>Interpreter preflight checks for script-path execution and stream download-to-exec command patterns.</li><li>Secret-path deny rules and zone-based filesystem boundaries to reduce direct exfil paths.</li><li>Receipts for blocked/allowed/pending sensitive operations.</li></ul><p>Recommended hardening when enabling internet:</p><ul><li>Keep <code>default_deny=true</code> unless internet is required.</li><li>If internet is required, use strict host allowlists and keep them minimal.</li><li>Run in agent-mode approval flows for high-risk transfer/delivery operations.</li><li>Review receipts during and after runs for unexpected network operations.</li></ul><p>Limitation note:</p><ul><li>v0.1.7 does not provide syscall-complete outbound mediation for arbitrary binaries without kernel-level hooks; command preflight primarily covers explicit URL-bearing command patterns.</li></ul>', 30)
  ])]);
}
const securityPolicy = /* @__PURE__ */ _export_sfc(_sfc_main, [["render", _sfc_render]]);
export {
  __pageData,
  securityPolicy as default
};
