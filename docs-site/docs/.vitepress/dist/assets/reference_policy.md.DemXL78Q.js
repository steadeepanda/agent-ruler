import { _ as _export_sfc, o as openBlock, c as createElementBlock, ag as createStaticVNode } from "./chunks/framework.UCBG75GM.js";
const __pageData = JSON.parse('{"title":"Policy Reference","description":"","frontmatter":{},"headers":[],"relativePath":"reference/policy.md","filePath":"reference/policy.md"}');
const _sfc_main = { name: "reference/policy.md" };
function _sfc_render(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("div", null, [..._cache[0] || (_cache[0] = [
    createStaticVNode('<h1 id="policy-reference" tabindex="-1">Policy Reference <a class="header-anchor" href="#policy-reference" aria-label="Permalink to &quot;Policy Reference&quot;">​</a></h1><p>Policy file: <code>state/policy.yaml</code></p><h2 id="profiles" tabindex="-1">Profiles <a class="header-anchor" href="#profiles" aria-label="Permalink to &quot;Profiles&quot;">​</a></h2><ul><li><code>strict</code> (default): strongest baseline; advanced + elevation customization locked.</li><li><code>simple_user</code>: safest daily-driver profile with most advanced controls locked.</li><li><code>balanced</code>: quick-start baseline; network/elevation controls open, advanced rule matrix locked.</li><li><code>coding_nerd</code>: coding-oriented profile; advanced controls unlocked.</li><li><code>i_dont_care</code>: emergency low-friction mode (not recommended for regular use).</li><li><code>custom</code>: full personalization profile.</li></ul><p>System-critical filesystem disposition is always forced to <code>deny</code> across all profiles. Secrets disposition is always forced to <code>deny</code>. Download-&gt;exec quarantine remains enforced as a minimum guard in every profile.</p><h2 id="recommended-profile-selection" tabindex="-1">Recommended profile selection <a class="header-anchor" href="#recommended-profile-selection" aria-label="Permalink to &quot;Recommended profile selection&quot;">​</a></h2><ul><li>Use <code>simple_user</code> if you want safety with minimal settings.</li><li>Use <code>balanced</code> if you want a practical default and faster onboarding.</li><li>Use <code>coding_nerd</code> for frequent development tasks with fewer interruptions.</li><li>Use <code>strict</code> for untrusted tasks or shared machines.</li><li>Use <code>i_dont_care</code> only for temporary unblocking.</li><li>Use <code>custom</code> when you intentionally manage all advanced settings.</li></ul><h2 id="rules" tabindex="-1">Rules <a class="header-anchor" href="#rules" aria-label="Permalink to &quot;Rules&quot;">​</a></h2><ul><li>Filesystem zone matrix (<code>allow</code>, <code>deny</code>, <code>approval</code>)</li><li>Network controls: <ul><li><code>default_deny</code></li><li><code>allowlist_hosts</code></li><li><code>require_approval_for_post</code> (approval gate for POST requests, including allowlisted hosts)</li><li><code>invert_allowlist</code> / <code>invert_denylist</code> (list interpretation toggles)</li></ul></li><li>Execution controls: workspace/tmp execution guards and download-&gt;exec quarantine</li><li>Persistence controls: deny and approval path sets</li><li>Elevation controls: <ul><li><code>enabled</code></li><li><code>require_operator_auth</code></li><li><code>use_allowlist</code></li><li><code>allowed_packages</code></li><li><code>denied_packages</code> (always enforced)</li></ul></li></ul><h2 id="safeguards" tabindex="-1">Safeguards <a class="header-anchor" href="#safeguards" aria-label="Permalink to &quot;Safeguards&quot;">​</a></h2><ul><li><code>mass_delete_threshold</code>: file-count threshold in a single delete operation that triggers approval.</li></ul><h2 id="approvals" tabindex="-1">Approvals <a class="header-anchor" href="#approvals" aria-label="Permalink to &quot;Approvals&quot;">​</a></h2><ul><li><code>ttl_seconds</code></li><li>Pending/resolution state tracked in approvals store</li><li>Long-poll waiting via <code>/api/approvals/&lt;id&gt;/wait</code></li></ul><h2 id="notes-on-package-policy" tabindex="-1">Notes on package policy <a class="header-anchor" href="#notes-on-package-policy" aria-label="Permalink to &quot;Notes on package policy&quot;">​</a></h2><ul><li>If <code>use_allowlist = true</code>, only packages in <code>allowed_packages</code> can pass (unless denylisted).</li><li>If <code>use_allowlist = false</code>, allowlist is skipped, but <code>denied_packages</code> still blocks.</li><li>If a package appears in both lists, denylist wins.</li></ul><h2 id="notes-on-network-list-defaults" tabindex="-1">Notes on network list defaults <a class="header-anchor" href="#notes-on-network-list-defaults" aria-label="Permalink to &quot;Notes on network list defaults&quot;">​</a></h2><ul><li>Domain lists are unchecked by default in UI presets.</li><li>Invert toggles decide whether checked hosts block or allow; unchecked lists do not create explicit host matches by themselves.</li><li>GET can remain broad (whole internet) when <code>default_deny = false</code> and confinement/network posture allows it.</li><li>POST remains more sensitive: allow only trusted domains and prefer manual human POST actions for sensitive write operations.</li><li>All network requests are still submitted to Agent Ruler enforcement (policy + confinement + approval gates).</li></ul>', 17)
  ])]);
}
const policy = /* @__PURE__ */ _export_sfc(_sfc_main, [["render", _sfc_render]]);
export {
  __pageData,
  policy as default
};
