import { _ as _export_sfc, o as openBlock, c as createElementBlock, ag as createStaticVNode } from "./chunks/framework.UCBG75GM.js";
const __pageData = JSON.parse('{"title":"Control Panel Guide","description":"","frontmatter":{},"headers":[],"relativePath":"guides/control-panel.md","filePath":"guides/control-panel.md"}');
const _sfc_main = { name: "guides/control-panel.md" };
function _sfc_render(_ctx, _cache, $props, $setup, $data, $options) {
  return openBlock(), createElementBlock("div", null, [..._cache[0] || (_cache[0] = [
    createStaticVNode('<h1 id="control-panel-guide" tabindex="-1">Control Panel Guide <a class="header-anchor" href="#control-panel-guide" aria-label="Permalink to &quot;Control Panel Guide&quot;">​</a></h1><p>This page is your practical tour of the WebUI. If you are still on setup, start with <a href="/help/guides/getting-started">Getting Started</a>.</p><h2 id="navigation-map" tabindex="-1">Navigation map <a class="header-anchor" href="#navigation-map" aria-label="Permalink to &quot;Navigation map&quot;">​</a></h2><ul><li><code>Overview</code>: runtime snapshot and quick actions</li><li><code>Approvals</code>: pending queue, single/bulk resolve, deep-link support</li><li><code>Import / Export</code>: import, stage, and deliver operations</li><li><code>Policy</code>: profile selection and configurable controls</li><li><code>Runtime Paths</code>: shared-zone and default delivery locations</li><li><code>Control Settings</code>: UI/debug/runtime Control Panel settings</li><li><code>Execution Layer</code>: one-shot troubleshooting runner, exec-layer reset, and full runtime reset</li><li><code>Timeline</code>: receipts and filtering</li></ul><p>You can collapse the left navigation to icon-only mode (or expand it back) from the sidebar/header toggle on any screen size.</p><p><code>Control Settings</code> includes:</p><ul><li><code>UI Bind Address</code></li><li><code>Show debug tools in UI</code></li><li><code>Allow degraded confinement fallback</code></li><li><code>Default Approval Wait Timeout (seconds)</code> (safe default <code>120</code>, range <code>1..300</code>)</li></ul><h2 id="policy-profile-quick-guide" tabindex="-1">Policy profile quick guide <a class="header-anchor" href="#policy-profile-quick-guide" aria-label="Permalink to &quot;Policy profile quick guide&quot;">​</a></h2><ul><li><code>Strict</code>: maximum guardrails, best for untrusted tasks.</li><li><code>Simple User</code>: safest everyday profile with minimal tuning required.</li><li><code>Balanced</code>: recommended quick-start profile for most operators.</li><li><code>Coding/Nerd</code>: more freedom for coding workflows while preserving core boundaries.</li><li><code>I DON&#39;T CARE</code>: emergency low-friction mode (&quot;you don&#39;t care, but I do lol&quot;).</li><li><code>Custom</code>: advanced personalization.</li></ul><p>Baseline protections always stay active, including system-critical deny, secrets deny, and download-&gt;exec quarantine.</p><h2 id="domain-rules-recommendation" tabindex="-1">Domain rules recommendation <a class="header-anchor" href="#domain-rules-recommendation" aria-label="Permalink to &quot;Domain rules recommendation&quot;">​</a></h2><ul><li>Keep POST domain entries limited to trusted endpoints you actually use.</li><li>Domain lists start unchecked; invert toggles decide whether checked hosts block or allow traffic, and every request still flows through Agent Ruler&#39;s policy/confinement checks.</li><li>GET can remain broad (whole internet) when <code>default_deny = false</code> and network posture allows it.</li><li>POST remains more sensitive: allow only trusted domains and prefer manual human POST actions for sensitive write operations.</li></ul><h2 id="import-stage-deliver-flow" tabindex="-1">Import / stage / deliver flow <a class="header-anchor" href="#import-stage-deliver-flow" aria-label="Permalink to &quot;Import / stage / deliver flow&quot;">​</a></h2><p>The zone layout intentionally shows two cards on the first row (<code>Workspace</code> + <code>Shared Zone</code>) and <code>Deliveries</code> on the next row to reduce visual overload.</p><ol><li><p>Import into workspace</p><ul><li>Choose a local file from the browser picker.</li><li>Destination override is optional and hidden until enabled.</li></ul></li><li><p>Stage from workspace to shared zone</p><ul><li>Pick source manually or from the workspace hierarchy browser.</li><li>Destination override is optional and hidden until enabled.</li></ul></li><li><p>Deliver from shared zone to destination</p><ul><li>Pick stage reference manually or from the shared-zone hierarchy browser.</li><li>Destination override is optional and hidden until enabled.</li></ul></li></ol><p>The hierarchy browser is zone-scoped (<code>workspace</code> / <code>shared-zone</code>) so transfer actions stay aligned with confinement boundaries. Use the zone browser scroll areas to inspect deeper folders before selecting stage/deliver targets.</p><h2 id="user-mode-vs-agent-mode" tabindex="-1">User mode vs agent mode <a class="header-anchor" href="#user-mode-vs-agent-mode" aria-label="Permalink to &quot;User mode vs agent mode&quot;">​</a></h2><ul><li>User mode (<code>auto_approve=true</code>): manual Control Panel actions skip approval queue.</li><li>Agent mode: same boundaries apply, but governed actions can enter approvals.</li><li>Drag/drop transfers are user-only; the <code>Auto-approve drag/drop transfers</code> checkbox controls whether those drop actions auto-approve or enter queue.</li></ul><h2 id="timeline-and-detailed-visibility" tabindex="-1">Timeline and detailed visibility <a class="header-anchor" href="#timeline-and-detailed-visibility" aria-label="Permalink to &quot;Timeline and detailed visibility&quot;">​</a></h2><p>Timeline shows receipts across governed actions.</p><ul><li>Filters: date, verdict, text search</li><li><code>Clear</code>: resets filters and shows full timeline</li><li>Default view: shows the details the agent can see (<code>decision.detail</code>)</li><li><code>Show operator-only debug details</code>: reveals hidden fields (full command + diff summary) for debugging</li><li><code>Use runtime path labels (recommended)</code>: shortens long paths in timeline entries (for example <code>WORKSPACE_PATH/...</code>)</li></ul><p>Operator-only fields stay hidden by default so shared timeline views remain safer.</p><h2 id="reset-options" tabindex="-1">Reset options <a class="header-anchor" href="#reset-options" aria-label="Permalink to &quot;Reset options&quot;">​</a></h2><p>From the <code>Execution Layer</code> page:</p><ul><li><code>Reset Execution Layer</code><ul><li>Clears only <code>state/exec-layer</code>.</li><li>Keeps config, policy, receipts, approvals, and staged exports.</li></ul></li><li><code>Reset Runtime</code> + <code>Keep current config and policy</code> checked <ul><li>Recreates runtime state while preserving your current config/policy wiring.</li></ul></li><li><code>Reset Runtime</code> + <code>Keep current config and policy</code> unchecked <ul><li>Restores default config/policy and recreates runtime state.</li></ul></li></ul><p>This makes recovery fast when something breaks, without forcing reinstall/reconfiguration every time.</p><h2 id="one-shot-command-runner" tabindex="-1">One-shot command runner <a class="header-anchor" href="#one-shot-command-runner" aria-label="Permalink to &quot;One-shot command runner&quot;">​</a></h2><p>The <code>Execution Layer</code> page includes a one-shot command runner for deterministic troubleshooting.</p><ul><li>Runs through <code>/api/run/script</code></li><li>Returns exit code, confinement mode, stdout, stderr</li><li>Writes receipts visible in Timeline</li></ul><p>Use this for operator diagnostics, not as a replacement for normal agent task pipelines.</p><h2 id="next-pages" tabindex="-1">Next pages <a class="header-anchor" href="#next-pages" aria-label="Permalink to &quot;Next pages&quot;">​</a></h2><ul><li>OpenClaw-focused setup and approvals workflow: <a href="/help/integrations/openclaw-guide">OpenClaw Guide</a></li><li>API-level integration and wait/resume flow: <a href="/help/integrations/openclaw-api-reference">OpenClaw API Reference</a></li><li>Validation checklist before unattended runs: <a href="/help/guides/manual-tests">Manual Tests</a></li></ul>', 32)
  ])]);
}
const controlPanel = /* @__PURE__ */ _export_sfc(_sfc_main, [["render", _sfc_render]]);
export {
  __pageData,
  controlPanel as default
};
